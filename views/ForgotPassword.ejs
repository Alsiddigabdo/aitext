<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>نسيت كلمة المرور | محلل النصوص الذكي</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@200;300;400;500;600;700;800&family=Pacifico&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.6.0/remixicon.min.css" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: { primary: "#8B5CF6", secondary: "#6366F1" },
                    borderRadius: { button: "8px" }
                }
            }
        };
    </script>
    <style>
        body { font-family: 'Cairo', sans-serif; background-color: #111827; color: #f3f4f6; }
        .glass-card { background: rgba(26, 36, 61, 0.8); backdrop-filter: blur(12px); border: 1px solid rgba(255, 255, 255, 0.15); }
        .animate-fade-in { animation: fadeIn 0.5s ease-in; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    </style>
</head>
<nav style="width:100%; margin-bottom:8vh;">
    <%- include('partials/navbar') %>
</nav>
<body class="min-h-screen flex items-center justify-center bg-gray-900">
    <div class="glass-card p-8 rounded-lg shadow-lg w-full max-w-md animate-fade-in">
        <div class="text-center mb-8">
            <h1 class="text-3xl font-['Pacifico'] text-primary mb-2">محلل النصوص</h1>
            <h2 class="text-xl font-bold">نسيت كلمة المرور</h2>
            <p class="text-gray-400 mt-2">أدخل بريدك الإلكتروني لإعادة تعيين كلمة المرور</p>
        </div>

        <form id="forgot-password-form" method="POST" action="/auth/forgot-password" class="space-y-6">
            <div>
                <label for="email" class="block text-sm font-medium text-gray-300 mb-2">البريد الإلكتروني</label>
                <div class="relative">
                    <input type="email" id="email" name="email" class="w-full p-3 rounded-lg bg-gray-800 border border-gray-700 focus:outline-none focus:ring-2 focus:ring-primary text-white" placeholder="أدخل بريدك الإلكتروني" required>
                    <i class="ri-mail-line absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                </div>
            </div>
            <button type="submit" id="forgot-btn" class="w-full py-3 rounded-button bg-primary text-white hover:bg-secondary transition flex items-center justify-center disabled:bg-gray-600">
                <i class="ri-mail-send-line ml-2"></i> إرسال رمز التحقق
            </button>
        </form>

        <% if (typeof message !== 'undefined' && message) { %>
            <div id="message" class="text-center mt-4 <%= success ? 'text-green-500' : 'text-red-500' %>">
                <%= message %>
            </div>
        <% } else { %>
            <div id="message" class="text-center mt-4 hidden"></div>
        <% } %>

        <div class="text-center mt-6">
            <p class="text-gray-400">العودة إلى <a href="/auth/login" class="text-primary hover:underline">تسجيل الدخول</a></p>
        </div>
    </div>
    <footer style="width:100%; margin-top:8vh;">
        <%- include('partials/footer') %>
    
    </footer>

</body>
</html>